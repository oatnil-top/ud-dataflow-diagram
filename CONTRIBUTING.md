# Contributing

**Issues are open** — bugs, sample graphs that render wrong, format questions.

**Pull requests are not accepted in this first release.** This is a scheduling decision, not a policy: the editor is consumed by a product that pins this repository by commit, and there is no reviewer bandwidth yet for external changes. It will be revisited; when it changes, this file and the README line change with it.
